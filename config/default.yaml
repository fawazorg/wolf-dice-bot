keyword: dice
app:
  defaultLanguage: en
  developerId: 82366923
  commandSettings:
    ignoreOfficialBots: true
    ignoreUnofficialBots: true
  networkSettings:
    retryMode: 1
    retryAttempts: 1

redis:
  host: localhost
  port: 6379
